import fs from"node:fs";import{createAccount,createClient}from"genlayer-js";import{studionet}from"genlayer-js/chains";import{TransactionStatus}from"genlayer-js/types";const account=createAccount(),c=createClient({chain:studionet,account}),code=fs.readFileSync("contracts/AgentMemoryAnchor.py","utf8"),hash=await c.deployContract({account,code,args:[]});console.log("deployment="+hash);const r:any=await c.waitForTransactionReceipt({hash:hash as never,status:TransactionStatus.FINALIZED,interval:5000,retries:180}),address=r.data?.contract_address??r.txDataDecoded?.contractAddress,f=(r.consensus_data?.leader_receipt??[]).filter((x:any)=>x.execution_result!=="SUCCESS"&&x.genvm_result?.error_code!=="CONSENSUS_VALIDATOR_QUORUM_REACHED");if(!address||r.result_name!=="MAJORITY_AGREE"||f.length)throw Error(JSON.stringify({address,f}));console.log(JSON.stringify({address,hash,status:r.status_name,consensus:r.result_name,contractExplorer:`https://explorer-studio.genlayer.com/address/${address}`,transactionExplorer:`https://explorer-studio.genlayer.com/tx/${hash}`},null,2));
